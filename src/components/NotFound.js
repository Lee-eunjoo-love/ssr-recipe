const NotFound = () => {
  return (
    <div
      style={{
        textAlign: "center",
        marginTop: "5rem",
      }}
    >
      <h2>Not found page.</h2>
    </div>
  );
};

export default NotFound;
